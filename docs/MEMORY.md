# MEMORY — foundation-dogfood (project, long-term index)

Curated project memory (what was decided for the product). Daily raw logs in `docs/memory/`.

- Sandbox to validate the Foundation pipeline; deployment = `direct`; stack = Node / `node --test`.
